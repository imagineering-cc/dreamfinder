name: dreamfinder
description: >-
  Dreamfinder — a Signal-based project management bot powered by Claude and MCP tools.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  anthropic_sdk_dart: ^0.3.1
  dart_mcp: ^0.4.1
  http: ^1.3.0
  dotenv: ^4.2.0
  meta: ^1.16.0
  sqlite3: ^2.7.0
  timezone: ^0.11.0

dev_dependencies:
  test: ^1.25.0
  mocktail: ^1.0.0
  lints: ^5.1.0
